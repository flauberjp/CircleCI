version: 2.1

orbs:
  docker: circleci/docker@1.7.0

jobs:
  example-job:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - docker/build:
          image: . 

workflows:
  example-workflow:
    jobs:
      - docker/publish:
          docker-password: DOCKER_PASSWORD 
          image: flauberjp/simple-node 

